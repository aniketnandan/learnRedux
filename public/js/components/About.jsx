import React from "react";

const About = React.createClass({
  render: function() {
    return (
      <span>Hello, you are here to see my app, so just see.</span>
    )
  }
});

export default About;
